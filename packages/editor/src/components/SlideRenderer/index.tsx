import SlideHost from "./SlideHost";

// console.log("SlideHost", SlideHost.toString());
function SlideRenderer(props: any) {
  // const selectedSlide: any = useAppSelector((state) => state["course"].selectedSlide);

  return <SlideHost />;
}

export default SlideRenderer;
